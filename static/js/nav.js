// nav.js — resalta el link activo según la ruta actual (multi-página)

function initNav() {
  // El link activo ya viene marcado desde el servidor con la clase "active"
  // en base.html mediante la variable active_section de Jinja2.
  // Este script solo gestiona el dropdown de cuenta.
  // (El smooth-scroll dentro de la misma página ya no aplica entre secciones.)
}
